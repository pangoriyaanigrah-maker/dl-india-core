__pycache__/
.claude/settings.local.json

# Import artifact, not a source of truth. book.json is generated from it and is
# what the dashboard actually reads — keeping both in git guarantees they drift,
# and a binary xlsx can't be diffed or reviewed in a PR.
Holdings_File.xlsx
